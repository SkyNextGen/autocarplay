pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    // ВАЖНО: разрешаем репозитории, объявленные в build.gradle (у тебя там google()/mavenCentral()).
    repositoriesMode.set(RepositoriesMode.PREFER_PROJECT)

    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "autocarplay"
include(":app")
