pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)

    repositories {
        google()
        mavenCentral()

        // Xposed repositories
        maven { url "https://repo.xposed.info/repo/" }
        maven { url "https://api.xposed.info/" }
    }
}

rootProject.name = "AutoCarPlayClicker"
include(":app")
